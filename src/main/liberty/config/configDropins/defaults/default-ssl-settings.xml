<server>
   <!-- SSL configuration when not in docker container -->
   <sslDefault outboundSSLRef="outboundSSLSettings" />
   <ssl id="outboundSSLSettings" keyStoreRef="defaultKeyStore" trustStoreRef="defaultKeyStore" trustDefaultCerts="true" />
</server>